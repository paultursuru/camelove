 <div class="container">
  <div class="row">
    <div class="col-6 card m-3 p-5">
      <h2 class=""><%= @animal.name %></h2>
      <% if @animal.dead %>
        <h2> Nos Vemos pronto </h2>

      <% else %>

        <p>My <%= @animal.breed %> is <%= @animal.age %></p>
        <p>It leaves in <%= @animal.city %></p>
        <p>Its hairs are <%= @animal.colour %></p>
        <h5>Here is more info to convince you :</h5>
        <p><%= @animal.description %></p>
        <p><%= @animal.price %>€/day</p>
    <!-- add cloudinary photo here -->
      <%= link_to "Update #{@animal.name}", edit_animal_path(@animal), class: "btn btn-info" %>
      <%= link_to "Kill #{@animal.name}", dead_animal_path(@animal), data: {confirm: "Killing #{@animal.name} ?"}, class: "mt-2 btn btn-danger"%>
      <%= link_to "Book #{@animal.name}", new_animal_booking_path(@animal), class: 'mt-2 btn btn-primary' %>
    </div>
    <% end %>
  </div>
</div>
